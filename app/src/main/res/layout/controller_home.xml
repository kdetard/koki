<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <ProgressBar
        android:id="@+id/progress"
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:layout_gravity="center" />

    <!--<androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:id="@+id/recycler_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            &lt;!&ndash;<eu.kanade.tachiyomi.ui.library.category.CategoryRecyclerView
                android:id="@+id/category_recycler"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="?actionBarSize"
                android:clipToPadding="false"
                android:visibility="invisible"
                android:paddingBottom="4dp"
                android:scrollbars="vertical" />&ndash;&gt;

            <View
                android:id="@+id/recycler_shadow"
                android:layout_width="match_parent"
                android:layout_height="20dp"
                android:alpha="0.25"
                android:background="@drawable/shape_gradient_top_shadow"
                android:translationY="12dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

            &lt;!&ndash;app:layout_constraintBottom_toTopOf="@id/recycler"&ndash;&gt;

            &lt;!&ndash;<include layout="@layout/library_grid_recycler"
                android:id="@+id/library_grid_recycler"/>&ndash;&gt;

            <View
                android:id="@+id/recycler_cover"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="0"
                android:background="@drawable/bottom_sheet_rounded_background"
                android:backgroundTint="?attr/background" />
        </FrameLayout>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>-->

</androidx.coordinatorlayout.widget.CoordinatorLayout>
